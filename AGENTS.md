# 项目开发指令

## 核心规则

- 修改范围必须限定在当前任务内，不主动添加未要求的功能。
- 保留用户已有修改，并遵循项目已经建立的实现模式。
- 前端代码禁止直接请求上游平台 API。
- 单个数据源失败不得影响其他数据源。
- 禁止提交密钥或敏感配置。
- 不直接 push 到 `main`；需要推送时使用 `dev/<slug>` 分支，并通过 Pull Request 合并。

## 上下文导航

只读取与当前任务相关的上下文：

- 简单局部任务：当前指令和相关代码即可。
- 新会话开始设计、编码或审查时：读取 `docs/PROJECT_CONTEXT.md`。
- 中高风险任务：先读取 `tasks/<id>/task.md`，再按其中清单加载文档。
- 产品、架构或多 Agent 任务：按需读取 `docs/PRD.md`、`docs/TECH_DESIGN.md` 或 `docs/AI_WORKFLOW.md`。

### 长文档读取规则

- 对有稳定章节结构的长文档，先用 `rg -n "^#{1,6} " <file>` 查看 1-6 级标题，再按任务读取相关章节。
- 如果文档没有清晰章节、章节标题无法判断内容、或任务涉及全局一致性，可以阅读全文。
- 如果局部读取后仍无法判断，应继续读取相邻章节或阅读全文。
- 不要为了节省 token 而牺牲正确性。

## 决策持久化

- Agent 应主动识别可能需要持久化的项目决策。
- 改变产品范围、公共接口、共享数据模型、跨模块约定、安全、部署或已有正式结论时，必须请求 Owner 确认。
- 阻塞决策立即询问；非阻塞决策在任务结束时集中询问。
- Owner 确认后，更新当前有效规则文档并记录 ADR；任务内部细节和临时方案不晋升。

## 验证要求

- 涉及接口、缓存、数据源或页面状态的修改，任务结束时必须说明已验证内容和未验证范围。
- 数据源相关修改至少关注：正常数据、单平台失败、缓存回退或错误状态。

## 本地服务管理

- 启动或重启前后端 dev server 前，先检查目标端口是否已有旧实例监听。
- 如果发现同一端口存在多个相关实例，必须先停掉旧实例，再启动或继续验证。
- 服务启动后，确认实际监听地址和端口；涉及本地访问差异时，同时验证 `127.0.0.1` 和 `localhost`。
- 任务结束时如保留本地服务运行，说明服务地址和已知 PID。
